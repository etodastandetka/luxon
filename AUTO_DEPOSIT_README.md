# Система автопополнения - Полное руководство

## 📋 Оглавление
1. [Обзор системы](#обзор-системы)
2. [Принцип работы](#принцип-работы)
3. [Архитектура](#архитектура)
4. [Ключевые особенности](#ключевые-особенности)
5. [Процесс автопополнения](#процесс-автопополнения)
6. [API эндпоинты](#api-эндпоинты)
7. [Настройка и интеграция](#настройка-и-интеграция)
8. [Типичные проблемы и решения](#типичные-проблемы-и-решения)

---

## 🎯 Обзор системы

Система автопополнения работает **мгновенно** (секунда в секунду) благодаря двусторонней проверке:
- **При создании заявки** - проверяется, есть ли уже входящий платеж
- **При получении платежа** - проверяется, есть ли ожидающая заявка

Это гарантирует **мгновенное пополнение** независимо от того, что пришло первым - заявка или платеж.

---

## ⚙️ Принцип работы

### Двусторонняя синхронизация

```
┌─────────────────────────────────────────────────────────────┐
│                    ДВУСТОРОННЯЯ ПРОВЕРКА                     │
└─────────────────────────────────────────────────────────────┘

Сценарий 1: Сначала создается заявка
───────────────────────────────────────
Пользователь создает заявку → Проверка входящих платежей → Найдено? → Автопополнение

Сценарий 2: Сначала приходит платеж
───────────────────────────────────────
Платеж поступает в систему → Проверка ожидающих заявок → Найдено? → Автопополнение
```

### Ключевые параметры

- **Окно времени**: 5 минут (300 секунд)
- **Точность совпадения**: до 1 копейки (0.01)
- **Статус заявки**: только `pending`
- **Тип заявки**: только `deposit`
- **Платеж**: только `isProcessed = false`

---

## 🏗️ Архитектура

### Основные компоненты

```
┌──────────────────────────────────────────────────────────────┐
│                     КОМПОНЕНТЫ СИСТЕМЫ                        │
└──────────────────────────────────────────────────────────────┘

1. lib/auto-deposit.ts
   └── matchAndProcessPayment() - ЕДИНСТВЕННАЯ функция автопополнения
       • Поиск совпадений
       • Пополнение баланса
       • Обновление статусов
       • Отправка уведомлений

2. lib/deposit-balance.ts
   └── depositToCasino() - Пополнение через API казино
       • Поддержка: 1xbet, Melbet, Winwin, 888starz, Mostbet, 1win
       • Проверка на дублирование
       • Обработка ошибок API

3. app/api/payment/route.ts
   └── POST /api/payment - Создание заявки
       • Проверка входящих платежей при создании
       • Мгновенное автопополнение

4. app/api/incoming-payment/route.ts
   └── POST /api/incoming-payment - Получение платежа
       • Проверка ожидающих заявок
       • Мгновенное автопополнение
```

---

## 🔑 Ключевые особенности

### 1. Мгновенное выполнение (Синхронное)

**ВАЖНО**: Используется `await` для синхронного выполнения, а не асинхронные задачи.

```typescript
// ✅ ПРАВИЛЬНО - синхронное выполнение
const result = await matchAndProcessPayment(paymentId, amount)
if (result && result.success) {
  // Автопополнение уже выполнено
}

// ❌ НЕПРАВИЛЬНО - асинхронное выполнение (замедляет)
matchAndProcessPayment(paymentId, amount).then(result => {
  // Это будет выполнено позже
})
```

### 2. Окно 5 минут

Защита от случайного пополнения старых заявок:

```typescript
const fiveMinutesAgo = new Date(Date.now() - 5 * 60 * 1000)

// Ищем только заявки за последние 5 минут
where: {
  createdAt: { gte: fiveMinutesAgo },
  status: 'pending',
  requestType: 'deposit'
}
```

### 3. Точное совпадение суммы

Точность до 1 копейки:

```typescript
const diff = Math.abs(reqAmount - amount)
const matches = diff < 0.01 // Разница не более 1 копейки
```

### 4. Атомарные транзакции

Гарантия консистентности данных:

```typescript
const updateResult = await prisma.$transaction(async (tx) => {
  // Проверяем что заявка все еще pending
  const currentRequest = await tx.request.findUnique(...)
  if (currentRequest?.status !== 'pending') {
    return { skipped: true }
  }
  
  // Обновляем заявку и платеж атомарно
  const [updatedRequest, updatedPayment] = await Promise.all([
    tx.request.update({ status: 'autodeposit_success' }),
    tx.incomingPayment.update({ isProcessed: true })
  ])
  
  return { updatedRequest, updatedPayment }
})
```

### 5. Защита от дублирования

Проверка на повторные пополнения:

```typescript
// Ищем пополнения за последние 5 минут
const recentDeposits = await prisma.request.findMany({
  where: {
    accountId: accountId,
    bookmaker: bookmaker,
    status: { in: ['completed', 'autodeposit_success'] },
    processedAt: { gte: fiveMinutesAgo }
  }
})

// Проверяем на дубликат по сумме
const duplicate = recentDeposits.find(deposit => 
  Math.abs(deposit.amount - amount) < 0.01
)

if (duplicate) {
  return { success: false, message: 'Депозит уже был проведен' }
}
```

---

## 🔄 Процесс автопополнения

### Полный цикл автопополнения

```
┌──────────────────────────────────────────────────────────────┐
│              ШАГИ АВТОПОПОЛНЕНИЯ (ДЕТАЛЬНО)                   │
└──────────────────────────────────────────────────────────────┘

ШАГ 1: Триггер (Создание заявки ИЛИ получение платежа)
────────────────────────────────────────────────────────
   ↓
   ├─→ Сценарий A: POST /api/payment (создание заявки)
   │   └─→ Проверка: есть ли входящие платежи с такой суммой?
   │
   └─→ Сценарий B: POST /api/incoming-payment (получение платежа)
       └─→ Проверка: есть ли ожидающие заявки с такой суммой?

ШАГ 2: Поиск совпадений (matchAndProcessPayment)
─────────────────────────────────────────────────
   ↓
   • Ищем заявки: status='pending', requestType='deposit', createdAt >= (now - 5 min)
   • Ищем платежи: isProcessed=false, paymentDate >= (now - 5 min)
   • Фильтруем по точному совпадению суммы (diff < 0.01)
   • Берем самую старую заявку (orderBy: createdAt ASC, take: 1)

ШАГ 3: Проверка валидности
───────────────────────────
   ↓
   • Заявка должна иметь: accountId, bookmaker, amount
   • Заявка не должна иметь обработанный платеж
   • Заявка должна быть создана не более 5 минут назад

ШАГ 4: Пополнение через API казино
────────────────────────────────────
   ↓
   • Проверка на дублирование (пополнения за последние 5 минут)
   • Вызов depositToCasino(bookmaker, accountId, amount)
   • Получение результата от API казино
   • Обработка ошибок API

ШАГ 5: Обновление статусов (транзакция)
─────────────────────────────────────────
   ↓
   • Проверка что заявка все еще pending
   • Проверка что платеж не обработан
   • Атомарное обновление:
     - request.status = 'autodeposit_success'
     - request.processedBy = 'автопополнение'
     - request.processedAt = now()
     - incomingPayment.isProcessed = true
     - incomingPayment.requestId = request.id

ШАГ 6: Верификация
───────────────────
   ↓
   • Проверка что статус действительно обновился
   • Логирование успеха/ошибки

ШАГ 7: Уведомление пользователю
────────────────────────────────
   ↓
   • Отправка уведомления в Telegram бот
   • Формат: "✅ Ваш баланс пополнен! 💰 Сумма: X сом"
```

---

## 📡 API эндпоинты

### 1. POST /api/payment

**Создание заявки на пополнение**

```typescript
// Request Body
{
  type: 'deposit',
  userId: '123456789',
  bookmaker: '1xbet',
  accountId: '12345',
  amount: 1000
}

// Response
{
  success: true,
  data: {
    id: 123,
    status: 'autodeposit_success', // или 'pending'
    ...
  }
}
```

**Логика при создании заявки:**

```typescript
// После создания заявки - МГНОВЕННАЯ проверка
const incomingPayments = await prisma.incomingPayment.findMany({
  where: {
    isProcessed: false,
    amount: requestAmount,
    paymentDate: { gte: new Date(Date.now() - 5 * 60 * 1000) }
  }
})

if (incomingPayments.length > 0) {
  // Мгновенное автопополнение
  await matchAndProcessPayment(incomingPayments[0].id, requestAmount)
}
```

### 2. POST /api/incoming-payment

**Получение входящего платежа**

```typescript
// Request Body
{
  amount: 1000,
  bank: 'О!Деньги',
  paymentDate: '2024-01-15T10:30:00Z',
  notificationText: 'Пополнение счета'
}

// Response
{
  success: true,
  data: {
    id: 456,
    amount: '1000',
    isProcessed: true // если автопополнение сработало
  }
}
```

**Логика при получении платежа:**

```typescript
// После сохранения платежа - МГНОВЕННАЯ проверка
const result = await matchAndProcessPayment(paymentId, amount)
if (result && result.success) {
  // Автопополнение выполнено мгновенно
}
```

### 3. POST /api/auto-deposit/match

**Ручной запуск автопополнения (для отладки)**

```typescript
// Request Body (опционально)
{
  paymentId: 456 // если нужно обработать конкретный платеж
}

// Response
{
  success: true,
  data: {
    processed: 1,
    failed: 0,
    results: [
      { paymentId: 456, result: { requestId: 123, success: true } }
    ]
  }
}
```

---

## ⚙️ Настройка и интеграция

### 1. Структура базы данных

**Таблица Request:**
```sql
CREATE TABLE Request (
  id SERIAL PRIMARY KEY,
  userId BIGINT NOT NULL,
  requestType VARCHAR(20) NOT NULL, -- 'deposit' или 'withdraw'
  status VARCHAR(50) NOT NULL, -- 'pending', 'autodeposit_success', 'completed', etc.
  statusDetail VARCHAR(255),
  bookmaker VARCHAR(50),
  accountId VARCHAR(100),
  amount DECIMAL(10, 2),
  processedBy VARCHAR(100), -- 'автопополнение' или логин админа
  processedAt TIMESTAMP,
  createdAt TIMESTAMP DEFAULT NOW(),
  updatedAt TIMESTAMP DEFAULT NOW()
);
```

**Таблица IncomingPayment:**
```sql
CREATE TABLE IncomingPayment (
  id SERIAL PRIMARY KEY,
  amount DECIMAL(10, 2) NOT NULL,
  bank VARCHAR(100),
  paymentDate TIMESTAMP DEFAULT NOW(),
  notificationText TEXT,
  isProcessed BOOLEAN DEFAULT FALSE,
  requestId INTEGER REFERENCES Request(id),
  createdAt TIMESTAMP DEFAULT NOW()
);
```

### 2. Переменные окружения

```env
# API казино (пример для 1xbet)
1XBET_HASH=your_hash_here
1XBET_CASHIERPASS=your_password
1XBET_LOGIN=your_login
1XBET_CASHDESKID=your_cashdesk_id

# Telegram бот (для уведомлений)
BOT_TOKEN=your_bot_token

# База данных
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
```

### 3. Интеграция в ваш проект

**Шаг 1: Скопируйте файлы**

```
lib/auto-deposit.ts         → ваша_папка/lib/auto-deposit.ts
lib/deposit-balance.ts      → ваша_папка/lib/deposit-balance.ts
lib/casino-deposit.ts       → ваша_папка/lib/casino-deposit.ts (если есть)
```

**Шаг 2: Добавьте проверку при создании заявки**

```typescript
// В вашем API endpoint создания заявки
import { matchAndProcessPayment } from '@/lib/auto-deposit'

export async function POST(request: Request) {
  // ... создание заявки ...
  
  const newRequest = await prisma.request.create({ ... })
  
  // МГНОВЕННАЯ проверка автопополнения
  if (newRequest.requestType === 'deposit' && newRequest.amount) {
    const requestAmount = parseFloat(newRequest.amount.toString())
    
    // Ищем входящие платежи с такой суммой
    const incomingPayments = await prisma.incomingPayment.findMany({
      where: {
        isProcessed: false,
        amount: requestAmount,
        paymentDate: { gte: new Date(Date.now() - 5 * 60 * 1000) }
      },
      take: 1
    })
    
    if (incomingPayments.length > 0) {
      // Мгновенное автопополнение
      await matchAndProcessPayment(incomingPayments[0].id, requestAmount)
    }
  }
  
  return Response.json({ success: true, data: newRequest })
}
```

**Шаг 3: Добавьте проверку при получении платежа**

```typescript
// В вашем API endpoint получения платежа
import { matchAndProcessPayment } from '@/lib/auto-deposit'

export async function POST(request: Request) {
  const { amount } = await request.json()
  
  // Сохраняем платеж
  const payment = await prisma.incomingPayment.create({
    data: {
      amount: parseFloat(amount),
      isProcessed: false
    }
  })
  
  // МГНОВЕННАЯ проверка автопополнения
  const result = await matchAndProcessPayment(payment.id, parseFloat(amount))
  
  if (result && result.success) {
    console.log(`✅ Автопополнение выполнено для платежа ${payment.id}`)
  }
  
  return Response.json({ success: true, data: payment })
}
```

**Шаг 4: Настройте API казино**

Убедитесь что функция `depositToCasino()` поддерживает ваши казино. Если нужно добавить новое казино:

```typescript
// В lib/deposit-balance.ts
export async function depositToCasino(bookmaker: string, accountId: string, amount: number) {
  // ... существующий код ...
  
  // Добавьте поддержку нового казино
  if (bookmaker === 'новое_казино') {
    const config = await getCasinoConfig(bookmaker)
    return await depositNewCasinoAPI(accountId, amount, config)
  }
}
```

---

## 🔧 Типичные проблемы и решения

### Проблема 1: Автопополнение не срабатывает

**Причины:**
- Заявка создана более 5 минут назад
- Сумма не совпадает (разница > 0.01)
- Платеж уже обработан (`isProcessed = true`)
- Заявка не в статусе `pending`

**Решение:**
```typescript
// Проверьте логи
console.log(`🔍 [Auto-Deposit] Request age: ${requestAge}ms`)
console.log(`🔍 [Auto-Deposit] Amount diff: ${Math.abs(reqAmount - amount)}`)

// Убедитесь что:
// 1. Заявка создана < 5 минут назад
// 2. Суммы точно совпадают
// 3. Статус заявки = 'pending'
// 4. Платеж isProcessed = false
```

### Проблема 2: Двойное пополнение

**Причина:** Не используется транзакция для обновления статусов

**Решение:**
```typescript
// ✅ ПРАВИЛЬНО - используйте транзакцию
await prisma.$transaction(async (tx) => {
  const currentRequest = await tx.request.findUnique({ where: { id } })
  if (currentRequest?.status !== 'pending') {
    return { skipped: true } // Уже обработано
  }
  
  await tx.request.update({
    where: { id },
    data: { status: 'autodeposit_success' }
  })
})
```

### Проблема 3: Медленное автопополнение

**Причины:**
- Используется асинхронное выполнение вместо синхронного
- Много запросов к БД
- Медленный API казино

**Решение:**
```typescript
// ✅ ПРАВИЛЬНО - синхронное выполнение
const result = await matchAndProcessPayment(paymentId, amount)

// ❌ НЕПРАВИЛЬНО - асинхронное выполнение
matchAndProcessPayment(paymentId, amount).then(result => {
  // Это будет выполнено позже
})
```

### Проблема 4: Ошибка API казино

**Причина:** Неправильные credentials или API недоступно

**Решение:**
```typescript
// Проверьте конфигурацию
const config = await getCasinoConfig(bookmaker)
console.log('Config:', config) // Не должно быть null

// Проверьте ошибку API
try {
  const result = await depositToCasino(bookmaker, accountId, amount)
  if (!result.success) {
    console.error('API Error:', result.message)
  }
} catch (error) {
  console.error('Deposit error:', error)
}
```

---

## 📊 Мониторинг и логирование

### Ключевые логи для отслеживания

```typescript
// Успешное автопополнение
✅ [Auto-Deposit] SUCCESS: Request 123 → autodeposit_success (verified)

// Найдено совпадение
✅ [Auto-Deposit] Exact match: Request 123 (1000) ≈ Payment 456 (1000)

// Нет совпадений
ℹ️ [Auto-Deposit] No exact matches found for payment 456 (amount: 1000)

// Ошибка API
❌ [Auto-Deposit] Deposit failed: Error message from API

// Пропущено (уже обработано)
⚠️ [Auto-Deposit] Request 123 already processed (status: autodeposit_success)
```

### Метрики для мониторинга

- Количество автопополнений в час
- Среднее время автопополнения
- Процент успешных автопополнений
- Количество ошибок API казино
- Количество пропущенных автопополнений (старые заявки)

---

## 🎯 Лучшие практики

1. **Всегда используйте синхронное выполнение** (`await`)
2. **Используйте транзакции** для атомарности обновлений
3. **Проверяйте статусы** перед обновлением
4. **Логируйте все шаги** для отладки
5. **Используйте окно 5 минут** для защиты от случайных пополнений
6. **Проверяйте на дублирование** перед пополнением через API
7. **Обрабатывайте ошибки API** корректно

---

## 📝 Заключение

Система автопополнения работает **мгновенно** благодаря:
- Двусторонней проверке (заявка ↔ платеж)
- Синхронному выполнению (`await`)
- Атомарным транзакциям
- Оптимизированным запросам к БД

Ключевое отличие от медленных систем - **проверка происходит в момент создания заявки И в момент получения платежа**, что гарантирует мгновенное пополнение в любом сценарии.

---

**Автор:** Система разработана для мгновенного автопополнения  
**Дата:** 2024  
**Версия:** 1.0


