<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <TextView
                android:id="@+id/descriptionText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Отключите приложения, уведомления от которых не нужно обрабатывать. Все уведомления сохраняются в историю независимо от статуса."
                android:textSize="14sp"
                android:textColor="@color/text_secondary_light"
                android:padding="16dp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/bg_rounded"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/emptyStateText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Приложения с уведомлениями пока не обнаружены.\n\nКак только придет первое уведомление от приложения, оно появится в этом списке."
                android:textSize="14sp"
                android:textColor="@color/text_secondary_light"
                android:gravity="center"
                android:padding="24dp"
                android:visibility="visible"
                android:lineSpacingExtra="4dp"
                app:layout_constraintTop_toBottomOf="@id/descriptionText"
                app:layout_constraintBottom_toBottomOf="parent" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipToPadding="false"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:visibility="gone"
                android:nestedScrollingEnabled="false"
                app:layout_constraintTop_toBottomOf="@id/descriptionText" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.core.widget.NestedScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>

