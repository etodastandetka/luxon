# Быстрая настройка с Timeweb ⚡

## У тебя уже есть готовые шаблоны в Timeweb! Используй их:

### Вариант 1: PHP (уже выбран) ⭐

1. **Скопируй код подключения из Timeweb:**
   - В панели Timeweb выбран "PHP"
   - Скопируй весь PHP код который там показан

2. **Создай файл:**
   ```bash
   sudo nano /var/www/html/api/payments.php
   ```

3. **Вставь код из Timeweb в начало, затем добавь код из `api_timeweb.php`:**
   - Код подключения из Timeweb (в начале)
   - Остальная логика INSERT из `api_timeweb.php`

4. **Проверь:**
   ```bash
   curl -X POST http://ТВОЙ_СЕРВЕР/api/payments.php \
     -H "Content-Type: application/json" \
     -d '{"bank_name":"Test","package_name":"test","amount":100,"raw_text":"test"}'
   ```

---

### Вариант 2: NodeJS (если есть сервер с Node.js)

1. **Скопируй код подключения из Timeweb:**
   - Выбери "NodeJS" в панели
   - Скопируй код подключения

2. **Обнови `api_server.js`:**
   - Замени секцию `new Pool({...})` на код из Timeweb
   - Добавь SSL если требуется в Timeweb

3. **Запусти:**
   ```bash
   node api_server.js
   ```

---

### Вариант 3: Python (если есть Python на сервере)

1. **Скопируй код подключения из Timeweb:**
   - Выбери "Python"
   - Используй готовый код

2. **Создай Flask API** как показывал ранее

---

## Что самое главное:

✅ **Timeweb уже дал тебе правильный код подключения!**

Просто:
1. Скопируй код подключения из Timeweb
2. Используй его в `api.php` или `api_server.js`
3. Добавь логику INSERT (уже готова в проекте)

---

## Рекомендация:

Если файл `api.php` уже создан через nano:
- Открой его: `sudo nano /var/www/html/api/payments.php`
- Замени секцию подключения на код из Timeweb
- Остальное оставь как есть

Готово! 🚀

