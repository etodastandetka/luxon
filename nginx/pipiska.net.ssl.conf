# Конфигурация Nginx для админки pipiska.net с SSL
# ИСПОЛЬЗУЙТЕ ЭТУ КОНФИГУРАЦИЮ ПОСЛЕ ПОЛУЧЕНИЯ SSL СЕРТИФИКАТОВ
# Скопируйте этот файл поверх pipiska.net.conf после получения сертификатов

# HTTP редирект на HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name pipiska.net www.pipiska.net;

    # Для Let's Encrypt certbot
    location ~ /.well-known/acme-challenge {
        allow all;
        root /var/www/html;
    }

    # Редирект на HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# HTTPS конфигурация
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name pipiska.net www.pipiska.net;

    # SSL сертификаты (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/pipiska.net/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/pipiska.net/privkey.pem;
    
    # SSL настройки
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Логи
    access_log /var/log/nginx/pipiska.net.access.log;
    error_log /var/log/nginx/pipiska.net.error.log;

    # Максимальный размер загружаемого файла
    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

