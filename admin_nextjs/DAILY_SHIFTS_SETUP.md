# Настройка системы дневных смен

## Описание

Система дневных смен автоматически считает статистику за каждый день с 00:00 до 23:59, рассчитывает чистую прибыль (8% от пополнений + 2% от выводов) и сохраняет данные о каждой смене.

**Автоматическое закрытие смен встроено в код приложения** - никаких дополнительных настроек не требуется!

## Установка

### 1. Установить зависимости

```bash
cd admin_nextjs
npm install
```

### 2. Применить миграцию базы данных

```bash
npx prisma db push
# или
npx prisma migrate dev --name add_daily_shifts
```

### 3. Готово! 🎉

**Всё!** Автоматическое закрытие смен уже встроено в код. При запуске приложения планировщик автоматически запустится и будет закрывать смену каждый день в 23:59.

Никаких дополнительных настроек, PM2 конфигураций или cron задач не требуется!

## Как это работает

### Автоматическое закрытие смен

При запуске Next.js приложения автоматически запускается планировщик (`lib/shift-scheduler.ts`), который:
- Использует `node-cron` для планирования задач
- Каждый день в 23:59 автоматически закрывает смену
- Собирает статистику за день (00:00-23:59)
- Рассчитывает чистую прибыль: 8% от пополнений + 2% от выводов
- Сохраняет запись в таблице `daily_shifts`

**Никаких дополнительных действий не требуется!** Планировщик запускается автоматически при старте приложения.

### Текущая смена (без выбора периода)

- Показывается статистика с 00:00 сегодня до текущего момента
- Чистая прибыль рассчитывается: 8% от пополнений + 2% от выводов
- Смена автоматически закрывается в 23:59

### Выбранный период в календаре

- Показываются все закрытые смены за выбранный период
- Статистика суммируется из всех смен
- Чистая прибыль = сумма чистой прибыли всех смен
- Если текущий день попадает в период, добавляется текущая смена

## Ручное закрытие смены

Если нужно закрыть смену вручную (например, для вчерашнего дня):

**Через скрипт:**
```bash
cd admin_nextjs
npx tsx scripts/close-daily-shift.ts
```

**Через API (требуется авторизация):**
```bash
POST /api/shifts/close
{
  "date": "2025-01-20" // Дата в формате YYYY-MM-DD
}
```

**Через UI:**
На странице `/dashboard/limits` есть кнопка "Закрыть смену" для текущей смены.

## Автоматическое закрытие

Смена **автоматически закрывается каждый день в 23:59** через PM2 cron. Никаких дополнительных действий не требуется после настройки PM2.

## API Endpoints

### GET /api/shifts
Получить смены за период:
```
GET /api/shifts?start=2025-01-01&end=2025-01-31
```

### POST /api/shifts/close
Закрыть смену за указанную дату (требуется авторизация):
```
POST /api/shifts/close
{
  "date": "2025-01-20"
}
```

### POST /api/shifts/auto-close
Автоматическое закрытие смены (для cron, опционально защищено CRON_SECRET):
```
POST /api/shifts/auto-close
Headers: X-Cron-Secret: YOUR_SECRET (если установлен CRON_SECRET в env)
```

## Структура данных смены

```typescript
{
  id: number
  date: string // YYYY-MM-DD
  depositsSum: string // Сумма пополнений
  withdrawalsSum: string // Сумма выводов
  netProfit: string // Чистая прибыль (8% + 2%)
  depositsCount: number // Количество пополнений
  withdrawalsCount: number // Количество выводов
  isClosed: boolean // Закрыта ли смена
  closedAt?: string // Когда была закрыта
}
```

## Проверка работы

1. Запустите приложение: `npm run dev` или `npm start`
2. В консоли вы должны увидеть: `✅ [Shift Scheduler] Планировщик запущен. Смена будет закрываться автоматически каждый день в 23:59`
3. Откройте страницу `/dashboard/limits`
4. Без выбора периода - увидите текущую смену
5. Выберите период в календаре - увидите все закрытые смены за период
6. В 23:59 каждый день в логах появится сообщение о закрытии смены

## Устранение неполадок

### Смена не закрывается автоматически

1. Убедитесь что приложение запущено (планировщик работает только когда приложение работает)
2. Проверьте логи приложения - должно быть сообщение о запуске планировщика
3. Проверьте что `node-cron` установлен: `npm list node-cron`
4. Убедитесь что приложение работает в production режиме или dev режиме (не в браузере)

### Неправильный расчет прибыли

- Убедитесь, что используются правильные статусы:
  - Пополнения: `autodeposit_success`, `auto_completed`
  - Выводы: `completed`, `approved`, `autodeposit_success`, `auto_completed`

### Данные не отображаются

- Проверьте, что миграция применена: `npx prisma studio` и проверьте таблицу `daily_shifts`
- Проверьте консоль браузера на ошибки
- Проверьте логи API: `pm2 logs` (если используете PM2)

