# Быстрый старт: Автоматическое закрытие смен

## 🚀 Автоматическое закрытие смен встроено в код!

Смена будет **автоматически закрываться каждый день в 23:59** без вашего участия. Всё работает прямо в коде приложения!

## Шаги для запуска:

### 1. Установить зависимости (если еще не установлены)

```bash
cd admin_nextjs
npm install
```

### 2. Применить миграцию базы данных

```bash
npx prisma db push
```

### 3. Запустить приложение

```bash
npm run dev
# или для production
npm start
```

**Готово!** При запуске приложения планировщик автоматически запустится и будет закрывать смену каждый день в 23:59.

## Как это работает:

1. При запуске Next.js приложения автоматически импортируется `lib/shift-scheduler.ts`
2. Планировщик использует `node-cron` для планирования задач
3. **Каждый день в 23:59** автоматически запускается закрытие смены
4. Скрипт собирает статистику за день (00:00-23:59)
5. Рассчитывает чистую прибыль: **8% от пополнений + 2% от выводов**
6. Сохраняет данные в таблицу `daily_shifts`
7. Смена помечается как закрытая

## Проверка работы:

1. Запустите приложение
2. В консоли должно появиться сообщение:
   ```
   ✅ [Shift Scheduler] Планировщик запущен. Смена будет закрываться автоматически каждый день в 23:59
   ```
3. Откройте `/dashboard/limits` в админке
4. Без выбора периода - увидите текущую смену (с 00:00 до текущего момента)
5. Выберите период в календаре - увидите все закрытые смены за период
6. В 23:59 каждый день в логах появится сообщение о закрытии смены

## Ручное закрытие (если нужно):

Если нужно закрыть смену вручную:

**Через UI:**
На странице `/dashboard/limits` есть кнопка "Закрыть смену" для текущей смены.

**Через API:**
```bash
POST /api/shifts/close
{
  "date": "2025-01-20"
}
```

## Важно:

- Планировщик работает только когда приложение запущено
- Если приложение перезапускается, планировщик автоматически запустится снова
- Никаких дополнительных настроек, PM2 или cron не требуется - всё работает в коде!

---

**Всё готово!** Просто запустите приложение и смена будет закрываться автоматически каждый день в 23:59.
