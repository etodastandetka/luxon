# Настройка Telegram бота для отправки ошибок

## Что это делает

Система автоматически отправляет критические ошибки из логов в ваш личный Telegram чат.

## Настройка

### 1. Получите свой Telegram Chat ID

**Способ 1: Через бота @userinfobot**
1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот пришлет ваш Chat ID (например: `123456789`)

**Способ 2: Через бота @getidsbot**
1. Откройте [@getidsbot](https://t.me/getidsbot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш Chat ID

**Способ 3: Через вашего бота**
1. Напишите вашему боту любое сообщение
2. Откройте: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
3. Найдите в ответе поле `"chat":{"id":123456789}` - это ваш Chat ID

### 2. Добавьте переменную окружения

Добавьте в файл `.env`:

```env
ADMIN_TELEGRAM_CHAT_ID=123456789
BOT_TOKEN=your_bot_token_here
```

Где:
- `ADMIN_TELEGRAM_CHAT_ID` - ваш личный Telegram Chat ID
- `BOT_TOKEN` - токен вашего Telegram бота (уже должен быть настроен)

### 3. Перезапустите сервер

```bash
pm2 restart luxon-ad
```

## Какие ошибки отправляются

Система автоматически отправляет:
- ❌ Критические ошибки (Prisma, API ошибки, 500 ошибки)
- ❌ Ошибки обработки webhook (crypto-pay, платежи)
- ⚠️ Предупреждения (502/503 от внешних API)
- 🔴 Критические системные ошибки

## Формат сообщений

```
❌ Ошибка в системе

Сообщение: [текст ошибки]
Время: 20.12.2025, 17:46:05
Контекст: Crypto Pay Webhook
URL: /api/crypto-pay/webhook

Stack trace:
[детали ошибки]
```

## Использование в коде

### Автоматическая отправка

Ошибки из `catch` блоков автоматически отправляются, если они критичные.

### Ручная отправка

```typescript
import { sendTelegramErrorNotification } from '@/lib/telegram-error-logger'

try {
  // ваш код
} catch (error: any) {
  await sendTelegramErrorNotification({
    message: error.message,
    stack: error.stack,
    context: 'My Function',
    severity: 'critical',
  })
}
```

### Отправка из клиента

```typescript
fetch('/api/error-log', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: 'Ошибка на клиенте',
    stack: error.stack,
    url: window.location.href,
    userAgent: navigator.userAgent,
    severity: 'error'
  })
})
```

## Проверка работы

1. Убедитесь, что `ADMIN_TELEGRAM_CHAT_ID` и `BOT_TOKEN` настроены
2. Создайте тестовую ошибку (например, в админке)
3. Проверьте, что сообщение пришло в ваш Telegram

## Отключение

Если нужно временно отключить отправку ошибок, просто удалите или закомментируйте `ADMIN_TELEGRAM_CHAT_ID` в `.env`.

