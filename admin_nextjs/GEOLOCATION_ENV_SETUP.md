# 🗺️ Настройка геолокации в .env файле

## Описание

Геолокационная защита использует три переменные окружения для определения разрешенной зоны доступа:

- `ALLOWED_LATITUDE` - широта центра разрешенной зоны
- `ALLOWED_LONGITUDE` - долгота центра разрешенной зоны  
- `ALLOWED_RADIUS_METERS` - радиус разрешенной зоны в метрах

## Быстрая настройка

### Вариант 1: Использование скрипта (рекомендуется)

```bash
cd /var/www/luxon/admin_nextjs
chmod +x scripts/set-geolocation-env.sh
./scripts/set-geolocation-env.sh
```

Скрипт запросит у вас координаты и автоматически добавит их в `.env` файл.

### Вариант 2: Ручная настройка

Добавьте следующие строки в файл `.env`:

```env
# Геолокация - разрешенная зона доступа
ALLOWED_LATITUDE=42.84877
ALLOWED_LONGITUDE=74.57028
ALLOWED_RADIUS_METERS=10
```

### Вариант 3: Через админ-панель

1. Войдите в админ-панель
2. Перейдите в раздел "Геолокация" (если есть)
3. Введите координаты и радиус
4. Сохраните настройки

## Примеры координат

### Бишкек, Кыргызстан (по умолчанию)
```env
ALLOWED_LATITUDE=42.84877
ALLOWED_LONGITUDE=74.57028
ALLOWED_RADIUS_METERS=1000
```

### Москва, Россия
```env
ALLOWED_LATITUDE=55.7558
ALLOWED_LONGITUDE=37.6173
ALLOWED_RADIUS_METERS=5000
```

### Алматы, Казахстан
```env
ALLOWED_LATITUDE=43.2220
ALLOWED_LONGITUDE=76.8512
ALLOWED_RADIUS_METERS=2000
```

## Как найти координаты

1. **Google Maps:**
   - Откройте https://www.google.com/maps
   - Найдите нужное место
   - Кликните правой кнопкой мыши на точку
   - Выберите координаты (они скопируются в буфер обмена)

2. **Yandex Maps:**
   - Откройте https://yandex.ru/maps
   - Найдите нужное место
   - Кликните правой кнопкой мыши
   - Выберите "Что здесь?" - координаты будут показаны

3. **Онлайн-сервисы:**
   - https://www.latlong.net/
   - https://www.gps-coordinates.net/

## Применение изменений

После изменения `.env` файла **обязательно перезапустите приложение**:

```bash
pm2 restart luxon-admin
```

Или если используете другой процесс-менеджер:

```bash
# Для systemd
sudo systemctl restart luxon-admin

# Для docker
docker-compose restart
```

## Приоритет настроек

1. **Переменные окружения (.env)** - высший приоритет
2. **База данных** - если переменные не заданы, используются значения из БД
3. **Значения по умолчанию** - если ничего не задано, используются координаты Бишкека

## Проверка настроек

После настройки проверьте, что переменные загружены:

```bash
cd /var/www/luxon/admin_nextjs
grep -E "ALLOWED_(LATITUDE|LONGITUDE|RADIUS)" .env
```

Должны быть видны три строки с вашими значениями.

## Устранение проблем

### Переменные не применяются

1. Проверьте, что файл `.env` находится в корне проекта `admin_nextjs/`
2. Убедитесь, что нет лишних пробелов вокруг `=`
3. Перезапустите приложение после изменений
4. Проверьте логи: `pm2 logs luxon-admin`

### Неверный формат координат

- Широта должна быть от -90 до 90
- Долгота должна быть от -180 до 180
- Радиус должен быть положительным числом

### Геолокация не работает

1. Проверьте, что API геолокации доступен в браузере
2. Убедитесь, что пользователь разрешил доступ к геолокации
3. Проверьте логи сервера на наличие ошибок

