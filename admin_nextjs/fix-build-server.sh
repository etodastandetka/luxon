#!/bin/bash
# Скрипт для исправления сборки на сервере
# Выполните на сервере в директории /var/www/luxon/admin_nextjs

echo "🔧 Исправление сборки админки и базы данных..."

# 1. Обновляем код из репозитория
echo "📥 Обновление кода..."
cd /var/www/luxon
git pull origin main

# 2. Переходим в директорию админки
cd admin_nextjs

# 3. КРИТИЧНО: Применяем Prisma миграцию для добавления колонки source
echo "🗄️ Применение Prisma миграции..."
npx prisma migrate deploy 2>/dev/null || {
    echo "⚠️ migrate deploy не сработал, используем db push..."
    npx prisma db push --accept-data-loss
}

# 4. Перегенерируем Prisma клиент
echo "🔄 Перегенерация Prisma клиента..."
npx prisma generate

# 5. Очищаем кеш сборки
echo "🧹 Очистка кеша..."
rm -rf .next
rm -rf node_modules/.cache

# 6. Пересобираем проект
echo "🏗️ Сборка проекта..."
npm run build

# 7. Перезапускаем PM2 процесс
echo "🔄 Перезапуск PM2 процесса..."
pm2 restart luxon-admin

echo "✅ Готово! Проверьте статус: pm2 status"

