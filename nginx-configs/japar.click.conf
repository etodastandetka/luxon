# Конфигурация Nginx для админки japar.click
# Скопируйте в /etc/nginx/sites-available/japar.click

server {
    listen 80;
    server_name japar.click www.japar.click;

    # Логи
    access_log /var/log/nginx/japar.click.access.log;
    error_log /var/log/nginx/japar.click.error.log;

    # Максимальный размер загружаемого файла
    client_max_body_size 50M;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# После получения SSL сертификата через certbot, этот блок будет автоматически добавлен:
# server {
#     listen 443 ssl http2;
#     server_name japar.click www.japar.click;
#     
#     ssl_certificate /etc/letsencrypt/live/japar.click/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/japar.click/privkey.pem;
#     
#     # SSL настройки
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     # Остальные настройки как в блоке выше
# }

