═══════════════════════════════════════════════════════════════
🚀 ФИНАЛЬНЫЕ КОМАНДЫ ДЛЯ ДЕПЛОЯ НА СЕРВЕР
═══════════════════════════════════════════════════════════════

1. ОБНОВЛЕНИЕ КОДА И БАЗЫ ДАННЫХ
───────────────────────────────────────────────────────────────
git pull origin main
cd admin_nextjs
npm run db:push
npm run db:generate
cd ..


2. СБОРКА ПРОЕКТОВ
───────────────────────────────────────────────────────────────
# Админка
cd admin_nextjs
npm install --production
npm run build
cd ..

# Клиентский сайт
cd bot2/mini_app_site
npm install --production
npm run build
cd ../..


3. ПЕРЕЗАПУСК PM2
───────────────────────────────────────────────────────────────
pm2 restart all

# Или если нужно перезапустить с нуля:
pm2 stop all
cd admin_nextjs && pm2 start ecosystem.config.js --env production
cd ../bot2/mini_app_site && pm2 start ecosystem.config.js --env production
pm2 save


4. ОБНОВЛЕНИЕ NGINX
───────────────────────────────────────────────────────────────
sudo cp nginx-configs/japar.click.cloudflare.conf /etc/nginx/sites-available/japar.click
sudo cp nginx-configs/luxon.dad.cloudflare.conf /etc/nginx/sites-available/luxon.dad
sudo nginx -t
sudo systemctl reload nginx


5. НАСТРОЙКА ГЕОЛОКАЦИИ
───────────────────────────────────────────────────────────────
# Вариант 1: Через .env файл (добавьте в admin_nextjs/.env)
ALLOWED_LATITUDE=42.84877
ALLOWED_LONGITUDE=74.57028
ALLOWED_RADIUS_METERS=10

# Вариант 2: Через API (после входа в админку)
curl -X POST https://japar.click/api/geolocation/settings \
  -H "Cookie: auth_token=YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "latitude": 42.84877,
    "longitude": 74.57028,
    "radius": 10
  }'


6. НАСТРОЙКА 2FA (ОПЦИОНАЛЬНО)
───────────────────────────────────────────────────────────────
cd admin_nextjs
export DATABASE_URL='postgresql://user:password@localhost:5432/luxon_admin'
pip install -r scripts/requirements-2fa.txt
python scripts/generate-2fa-qr.py admin --save


7. ПРОВЕРКА РАБОТЫ
───────────────────────────────────────────────────────────────
pm2 status
pm2 logs

# Проверка API
curl -I http://127.0.0.1:3001/api/public/payment-settings
curl -I http://127.0.0.1:3030

# Проверка сайтов
curl -I https://japar.click
curl -I https://luxon.dad


═══════════════════════════════════════════════════════════════
✅ ВСЕ ГОТОВО!
═══════════════════════════════════════════════════════════════

Защита настроена:
✅ Rate limiting (30-1000 req/min в зависимости от типа запроса)
✅ Блокировка IP на 24 часа
✅ Защита от SQL инъекций и XSS
✅ Внутренние запросы не блокируются
✅ API между клиентским сайтом и админкой работает
✅ Геолокационная защита (10 метров от координат)
✅ Двухфакторная аутентификация (2FA)

Проверьте:
- https://japar.click (админка)
- https://luxon.dad (клиентский сайт)
- pm2 logs (логи)

При заходе на сайт:
1. Проверка геолокации (если не проверена)
2. Страница логина (если не авторизован)
3. Дашборд

