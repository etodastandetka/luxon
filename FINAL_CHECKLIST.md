# ✅ Финальный чеклист перед деплоем

## Проверка кода

- [x] Нет ошибок линтера
- [x] Все зависимости установлены
- [x] Схема БД обновлена (добавлены поля для 2FA)
- [x] Защита не блокирует легитимные запросы
- [x] Внутренние запросы (127.0.0.1) пропускаются
- [x] Нормальные браузеры не блокируются

## Проверка защиты

- [x] Rate limiting настроен
- [x] Блокировка IP на 24 часа
- [x] Защита от SQL инъекций
- [x] Защита от XSS
- [x] Проверка User-Agent (не блокирует браузеры)
- [x] Cloudflare проверка (только в продакшене)
- [x] Внутренние запросы пропускаются

## Проверка API совместимости

### Публичные endpoints (доступны без авторизации):
- [x] `/api/public/payment-settings` - защищен, но доступен
- [x] `/api/public/requisites/list` - защищен, но доступен
- [x] `/api/public/referral-data` - защищен, но доступен
- [x] `/api/public/check-user-status` - защищен, но доступен
- [x] `/api/public/generate-qr` - защищен, но доступен
- [x] `/api/payment` - защищен, но доступен
- [x] `/api/withdraw-check` - защищен, но доступен
- [x] `/api/withdraw-execute` - защищен, но доступен
- [x] `/api/transaction-history` - защищен, но доступен
- [x] `/api/referral/register` - защищен, но доступен
- [x] `/api/channel/settings` - защищен, но доступен

### Внутренние запросы (127.0.0.1):
- [x] Клиентский сайт может обращаться к админке через `http://127.0.0.1:3001`
- [x] Внутренние запросы не блокируются защитой
- [x] Rate limiting для внутренних запросов: 1000 req/min

## Команды для сервера

### Быстрый деплой (одной командой):

```bash
bash SERVER_DEPLOY.sh
```

### Или пошагово:

```bash
# 1. Обновление кода
git pull origin main

# 2. Обновление БД
cd admin_nextjs
npm run db:push
npm run db:generate
cd ..

# 3. Сборка админки
cd admin_nextjs
npm install --production
npm run build
cd ..

# 4. Сборка клиентского сайта
cd bot2/mini_app_site
npm install --production
npm run build
cd ../..

# 5. Перезапуск PM2
pm2 restart all
# или
pm2 stop all
cd admin_nextjs && pm2 start ecosystem.config.js --env production
cd ../bot2/mini_app_site && pm2 start ecosystem.config.js --env production
pm2 save

# 6. Обновление Nginx
sudo cp nginx-configs/japar.click.cloudflare.conf /etc/nginx/sites-available/japar.click
sudo cp nginx-configs/luxon.dad.cloudflare.conf /etc/nginx/sites-available/luxon.dad
sudo nginx -t
sudo systemctl reload nginx

# 7. Проверка
pm2 status
curl -I http://127.0.0.1:3001/api/public/payment-settings
curl -I http://127.0.0.1:3030
```

## Проверка после деплоя

### 1. Проверка работы сайтов

```bash
# Админка
curl -I https://japar.click

# Клиентский сайт
curl -I https://luxon.dad
```

### 2. Проверка API

```bash
# Публичный API админки
curl https://japar.click/api/public/payment-settings

# Внутренний API (с сервера)
curl http://127.0.0.1:3001/api/public/payment-settings
```

### 3. Проверка защиты

```bash
# Должен вернуть 429 после 30 запросов
for i in {1..35}; do curl -I https://japar.click/api/payment; done

# Должен вернуть 403 (подозрительный User-Agent)
curl -H "User-Agent: curl/7.68.0" https://japar.click/api/payment
```

### 4. Проверка логов

```bash
# PM2 логи
pm2 logs

# Nginx логи
sudo tail -f /var/log/nginx/japar.click.access.log
sudo tail -f /var/log/nginx/luxon.dad.access.log
```

## Настройка 2FA (опционально)

```bash
cd admin_nextjs
export DATABASE_URL='postgresql://user:password@localhost:5432/luxon_admin'
pip install -r scripts/requirements-2fa.txt
python scripts/generate-2fa-qr.py admin --save
```

## Важные замечания

1. ✅ **Внутренние запросы (127.0.0.1) не блокируются** - клиентский сайт может обращаться к админке
2. ✅ **Нормальные браузеры не блокируются** - User-Agent проверка пропускает Chrome, Firefox, Safari, Telegram и т.д.
3. ✅ **Rate limiting для внутренних запросов: 1000 req/min** - достаточно для любых операций
4. ✅ **Все публичные API endpoints защищены**, но доступны для легитимных запросов
5. ✅ **Блокировка IP на 24 часа** при нарушении

## Структура защиты

### Уровень 1: Nginx
- Rate limiting (30 req/min для API, 120 для страниц)
- Ограничение соединений (20 с одного IP)
- Защита от Slowloris
- Блокировка подозрительных User-Agent

### Уровень 2: Next.js Middleware
- Проверка блокировки IP
- Защита от подозрительных URL
- Защита от подозрительных заголовков
- Защита от подозрительных параметров
- Валидация размера запроса
- Проверка Cloudflare (в продакшене)
- Rate limiting с автоматической блокировкой

### Уровень 3: API Endpoints
- Валидация входных данных
- Защита от SQL инъекций
- Защита от XSS
- Санитизация данных
- Rate limiting для критичных endpoints

### Уровень 4: 2FA (опционально)
- TOTP аутентификация
- Резервные коды
- QR код для настройки

## Геолокационная защита

- [x] Страница проверки геолокации создана (`/geolocation`)
- [x] API endpoint для проверки координат (`/api/geolocation/verify`)
- [x] API endpoint для настройки координат (`/api/geolocation/settings`)
- [x] Middleware проверяет геолокацию перед доступом
- [x] Координаты можно хранить в .env или БД
- [x] Формула гаверсинуса для расчета расстояния
- [x] Радиус: 10 метров (настраивается)
- [x] Cookie действительна 24 часа

### Настройка координат:

**Через .env:**
```env
ALLOWED_LATITUDE=42.84877
ALLOWED_LONGITUDE=74.57028
ALLOWED_RADIUS_METERS=10
```

**Через БД (API):**
```bash
curl -X POST https://japar.click/api/geolocation/settings \
  -H "Cookie: auth_token=YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"latitude": 42.84877, "longitude": 74.57028, "radius": 10}'
```

## Готово к деплою! 🚀

Все проверки пройдены, защита настроена, API совместимость обеспечена, геолокация работает.

