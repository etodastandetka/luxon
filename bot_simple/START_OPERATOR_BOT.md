# Инструкция по запуску бота оператора на сервере

## Способ 1: Через PM2 (рекомендуется)

### 1. Установка зависимостей

```bash
cd /var/www/luxon/bot_simple
bash install.sh
```

### 2. Настройка токена (опционально)

Если токен бота оператора отличается от дефолтного, установите переменную окружения:

```bash
export SUPPORT_BOT_TOKEN="ваш_токен_бота"
```

Или добавьте в `~/.bashrc` или `~/.profile`:
```bash
echo 'export SUPPORT_BOT_TOKEN="ваш_токен_бота"' >> ~/.bashrc
source ~/.bashrc
```

### 3. Запуск через PM2

```bash
cd /var/www/luxon/bot_simple
pm2 start ecosystem.config.js --only operator_bot
```

Или запустить все боты из конфига:
```bash
pm2 start ecosystem.config.js
```

### 4. Проверка статуса

```bash
pm2 status
pm2 logs operator_bot
```

### 5. Управление ботом

```bash
# Остановить
pm2 stop operator_bot

# Перезапустить
pm2 restart operator_bot

# Удалить из PM2
pm2 delete operator_bot

# Автозапуск при перезагрузке сервера
pm2 save
pm2 startup
```

## Способ 2: Прямой запуск (для тестирования)

### 1. Активация виртуального окружения

```bash
cd /var/www/luxon/bot_simple
source venv/bin/activate
```

### 2. Установка переменной окружения (если нужно)

```bash
export SUPPORT_BOT_TOKEN="ваш_токен_бота"
```

### 3. Запуск бота

```bash
python support_bot.py
```

Для запуска в фоне:
```bash
nohup python support_bot.py > operator_bot.log 2>&1 &
```

## Способ 3: Через systemd (альтернатива PM2)

Создайте файл `/etc/systemd/system/operator-bot.service`:

```ini
[Unit]
Description=Operator Bot for LUXON
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/www/luxon/bot_simple
Environment="PYTHONUNBUFFERED=1"
Environment="SUPPORT_BOT_TOKEN=ваш_токен_бота"
ExecStart=/var/www/luxon/bot_simple/venv/bin/python /var/www/luxon/bot_simple/support_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable operator-bot
sudo systemctl start operator-bot
sudo systemctl status operator-bot
```

## Проверка работы

После запуска проверьте логи:
```bash
# PM2
pm2 logs operator_bot --lines 50

# Прямой запуск
tail -f operator_bot.log

# systemd
sudo journalctl -u operator-bot -f
```

Бот должен вывести сообщение:
```
🤖 Бот техподдержки запущен!
```

## Важные замечания

1. **Токен бота**: Убедитесь, что токен правильный. Токен можно получить у @BotFather в Telegram.

2. **ID операторов**: В файле `support_bot.py` на строке 23 указаны ID операторов, которые будут получать уведомления:
   ```python
   OPERATOR_IDS = [7638996648, 6826609528, 8203434235]
   ```
   При необходимости измените эти ID.

3. **Зависимости**: Убедитесь, что все зависимости установлены:
   ```bash
   pip install -r requirements.txt
   ```

4. **Права доступа**: Убедитесь, что у скрипта есть права на выполнение:
   ```bash
   chmod +x support_bot.py
   ```

## Решение проблем

### Бот не запускается
- Проверьте токен: `echo $SUPPORT_BOT_TOKEN`
- Проверьте логи: `pm2 logs operator_bot`
- Проверьте, что виртуальное окружение активировано и зависимости установлены

### Бот падает
- Проверьте логи ошибок
- Убедитесь, что интернет-соединение стабильное
- Проверьте, что токен валидный

### Операторы не получают уведомления
- Проверьте ID операторов в коде
- Убедитесь, что операторы начали диалог с ботом (отправили /start)

